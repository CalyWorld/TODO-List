import Homepage from "./Homepage";


const Player = (name, marker) => {
    const getName = () => name;
    const getMarker = () => marker;
    return { getName, getMarker };
  };

  export default Logicpage.js;